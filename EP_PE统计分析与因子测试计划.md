# EP/PE 统计分析与因子测试计划

**日期：2026 年 9 月 14 日**

> **摘要**  本文汇总 EP/PE 因子的计算口径、描述性统计和申万一级行业 PE 分布，并给出 EP、PB 等估值因子的测试计划。当前已经完成因子结果验收、描述性分析和 EP 月度横截面 Huber RLM；IC、分层回测、OLS/WLS 对照、稳健性及交易成本检验仍属于后续计划。

---

## 一、第五周工作：EP、PB 等因子测试计划

在完成数据完整性、准确性和时效性比较后，下一步将检验估值因子是否能够稳定预测股票未来收益。测试依次包括测试前验收、截面回归、IC 检验、分层回测和稳健性检验。

处理前再次明确因子生产口径：不根据 ST/PT 标记或次新股标记筛除股票，次新标记只用于记录；主动置为缺失值的掩码仅来自停牌状态。此外，在行情表原始股票代码上排除 50 只 `900xxx.BJ` 上海 B 股和 39 只 `200/201xxx.SZ` 深圳 B 股。此前两库数据比较未筛除这 39 只深圳 B 股，将其计入了深市 A 股，本次计算已修正该口径。

因子生产口径与测试股票池口径应分开管理。本次因子横截面和后续测试池均按行情代码口径保留沪深 A 股及北交所股票；不筛除 ST/PT 和上市未满一年的股票。所有未来可能增加的筛选标记均须为历史时点可用数据，不能回写或重算已经交付的原始因子。北交所股票在 2025 年发生六位代码切换；本次利用休市前后的收盘价、前收盘价和总股本生成 242 对一一映射，日截面继续保留行情表当日代码，财报连接和跨期收益则按统一证券身份处理。

### 1.1 研究对象与因子方向

主测试因子采用盈利收益率：

```text
EP[i,t] = NP_TTM[i,t] / MV[i,t]
```

其中，`NP_TTM[i,t]` 为交易日 `t` 当时已经公开的 TTM 归母净利润，`MV[i,t]` 为当日总市值。EP 越大表示估值越低，预期未来收益可能越高。

交付的原始 PE 为 EP 的倒数，负利润对应负 PE：

```text
PE_raw[i,t] = MV[i,t] / NP_TTM[i,t] = 1 / EP[i,t]
```

传统市盈率测试仅在 `NP_TTM[i,t] > 0` 时进行。由于 PE 在净利润接近零时容易产生极端值，并且负 PE 不能被解释为正常估值倍数，本文以 EP 作为主因子，以正利润样本的 PE 作为稳健性检验。

PB 因子可沿用相同测试框架。为使因子方向与 EP 一致，主测试可使用 `BP = 1 / PB`，此时 BP 越高代表估值越低、预期收益越高；若直接使用 PB，则预期收益方向相反。

### 1.2 测试前验收

虽然数据清洗和因子计算已经完成，但在正式测试前仍需对最终因子表进行一次验收，防止数据问题传递至回测结果。

1. 在 MAD 去极值和标准化之前确定沪深纯 A 股和北交所测试池，排除 89 只沪深 B 股；**已实现**。
2. 保留退市股票在实际退市前的历史样本，避免幸存者偏差；**已实现**
3. 财务数据的公告时间不得晚于因子形成时间；同一报告期按公告时间确定版本，新公告生效后不允许旧公告覆盖；**已实现**。
4. 保留总市值、TTM 归母净利润、报告期、公告时间、原始 EP/PE 和标准化因子值，确保异常结果可以追溯；**已实现**，
5. 检查每日因子覆盖率，并从覆盖率稳定后的首个月末开始正式测试；**已实现**。 EP 覆盖率连续 3 个自然月末不低于 95%，首次确认日为 2020 年 7 月 31 日。

当前的行情表没有对st标记，并且行情表没有完整的上市日期，测试时无法对这两种情况进行筛除。

### 1.3 样本与前瞻收益率
1.每个月最后一个交易日形成因子值（之前对于报表的统计规律，在4,8,10月底集中公布财报）
2.测试组合定义为下一个月调仓日的收盘价。
前瞻收益率定义为：

```text
r_fwd[i,T+1] = P_adj[i,exit] / P_adj[i,entry] - 1
```

计算收益率时用后复权价格，即发生过分红，拆股，合股等时间是，股价会相应发生变化，但实际的收益不会随事件发生变化，要用复权价格表示。

### 1.4 分期截面回归

每个月对股票下一期收益率进行一次截面回归：

```text
r_fwd[i,t+1] = alpha_t + beta_t * X[i,t]
             + gamma_t * ln(MV[i,t])
             + sum(k=1,...,K-1){delta[k,t] * I[i,k,t]}
             + epsilon[i,t+1]
```

其中，`X[i,t]` 为标准化 EP，`I[i,k,t]` 为股票所属行业的哑变量，`ln(MV[i,t])` 用于控制市值效应，`beta_t` 为当期因子收益。

主结果采用 RLM 稳健回归，以降低异常收益率对结果的影响；后续再使用 OLS 和以市值平方根加权的 WLS 进行对照。统计指标包括：

- 因子收益均值及累计因子收益；
- 单月 RLM 系数的 H1 标准误及大样本 `z` 值；
- 月度因子收益均值的 IID 标准误、`t` 值和 `p` 值（本阶段不使用 Newey-West HAC）；
- `beta_t > 0` 的月份比例；
- 单期回归 `|z| >= 2` 的月份比例；
- RLM、OLS 结果的一致性。

对于 EP 或 BP 因子，预期回归系数为正；若直接使用 PE 或 PB，则预期系数为负。EP 的新口径 RLM 已完成，结果见第三部分；OLS/WLS 对照尚未运行。

### 1.5 Rank IC 检验

首先将 EP 对行业和对数市值进行截面回归，使用回归残差 `X_neutral[i,t]` 作为行业和市值中性化后的因子暴露。Rank IC 定义为：

```text
IC_t = Spearman(X_neutral[i,t], r_fwd[i,t+1])
IR   = mean(IC) / std(IC)
```

IC 检验应报告 IC 均值、标准差、`IC_t > 0` 的月份比例、`|IC_t| > 0.02` 的月份比例以及 IR。还应逐年统计上述指标，检查因子的预测能力是否集中在少数年份。本阶段不执行 Newey-West HAC 推断。

### 1.6 行业内分层回测

每个调仓日在各一级行业内部按照 EP 从低到高划分为五组，再将各行业相同序号的组别合并为全市场组合。主结果采用市值加权，同时报告等权结果。多空组合收益定义为：

```text
R_LS[t] = R_Q5[t] - R_Q1[t]
```

其中，`Q5` 为 EP 最高、估值最低的一组，`Q1` 为 EP 最低的一组。

各组及多空组合需要统计年化收益、累计收益、基准超额收益、年化波动率、夏普比率、最大回撤、信息比率、胜率和换手率。通过五组收益排列以及组序号与收益之间的 Spearman 相关系数检验因子单调性。

基准优先选择中证全指；若缺少指数数据，可使用测试股票池的市值加权收益作为内部基准。

### 1.7 交易成本与稳健性检验

根据组合换手率计算扣除交易成本后的收益，并设置不同单边成本情景。此外，还需要完成以下检验：

1. 分年度以及市场上涨、下跌阶段统计回归、IC 和多空收益；
2. 分别在大、中、小市值股票中进行测试；
3. 比较全样本 EP 与仅包含正利润公司的传统 PE 样本；
4. 比较等权与市值加权、五组与十组、T+1 与 T+2 生效结果；
5. 比较不同 MAD 参数下测试结论是否稳定；
6. 固定行情和股票池，分别使用 Wind 与 base 财报构造因子，比较两套因子的覆盖率、秩相关性、IC 和多空收益；
7. 将最后一段时间保留为样本外区间，不根据样本外结果调整参数。

### 1.8 综合评价标准

| 评价维度 | 核心指标 | 判断方法 |
|---|---|---|
| 有效性 | 因子收益、单月 H1 `z` 值及全期 IID `t` 值 | 收益方向符合预期，且具有统计与经济意义 |
| 预测能力 | Rank IC、IR | IC 方向稳定，均值不依赖少数极端月份 |
| 单调性 | 五组收益及 `Q5 - Q1` | 各组收益大体单调，多空收益方向正确 |
| 稳定性 | 年度及市场状态分组 | 结果不依赖少数月份或单一年份 |
| 可交易性 | 换手率及扣费后收益 | 扣除合理交易成本后仍具有正收益 |
| 数据稳健性 | Wind 与 base 测试结果 | 核心结论不因财报数据来源而反转 |

只有当截面回归、Rank IC 和分层回测的方向基本一致，样本外结果稳定，并且扣除交易成本后仍有收益时，才能认为 PE/EP 因子具有较可靠的实际选股能力。所有阈值和参数应在查看样本外结果之前固定，避免结果导向调参。

### 1.9 实施顺序与输出结果

> **测试前验收** → **前瞻收益构造** → **RLM/OLS/WLS 截面回归** → **Rank IC** → **行业内分层** → **稳健性与成本检验**

最终形成因子数据验收表、截面回归汇总表、IC 汇总表、五组及多空组合绩效表、年度稳定性表和两库因子测试对比表，作为判断该因子是否能够进入后续多因子模型的依据。

**当前状态：** 数据验收、描述性统计和 EP 月度横截面 RLM 已完成；其余测试待执行。

---

## 二、EP/PE 因子结果统计

### 2.1 样本与计算口径

样本期为 2020 年 1 月 2 日至 2025 年 12 月 31 日，共 1,455 个交易日。在行情表原始代码上排除 50 只 `900xxx.BJ` 上海 B 股和 39 只 `200/201xxx.SZ` 深圳 B 股后，样本包含 7,081,800 条股票日记录和 5,927 个全样本期唯一行情代码标识。其中沪市 2,382 个、深市 3,006 个、北交所 539 个。这里的 539 是全样本期代码标识数，不是北交所公司数：其中包括 251 个 `43/83/87xxxx.BJ` 历史代码和 288 个 `920xxx.BJ` 新代码；2025 年代码切换使同一证券的新旧代码被重复计数。2025 年 12 月 31 日行情截面共有 288 只北交所股票，均使用 `920xxx.BJ` 代码。实际排除的证券只有上述 89 只沪深 B 股，普通北交所股票全部保留。ST/PT 股票不作筛选，次新股标记仅用于记录，只有停牌日被主动置为缺失值。

原始因子定义为：

```text
EP[i,t] = TTM 归母净利润[i,t] / 总市值[i,t]
PE[i,t] = 1 / EP[i,t]
```

行情日 `t` 仅使用截至当日结束前已经可获得的财务信息，并将因子用于下一交易日。原始 EP 和 PE 各有 6,732,630 条有效观测，有效率均为 95.07%，点时未来数据违规为 0。北交所代码切换后，行情代码仍保存为 `920xxx.BJ`，但通过 `financial_code6` 连接同一证券旧代码下的财报历史。

### 2.2 数据验收结果

| 验收项目 | 结果 |
|---|---:|
| 排除 B 股 | 89 只：上海 B 股 50 只、深圳 B 股 39 只 |
| 北交所新旧代码映射 | 242 对，价格连续且一一对应 |
| 忽略旧公告回退记录 | 3,881 条 |
| 有效财报版本 | 262,984 条 |
| 逐行因子追溯记录 | 7,081,800 条 |
| 点时未来数据违规 | 0 条 |
| 覆盖率稳定规则 | 非停牌样本连续 3 个自然月末不低于 95% |
| 正式测试起点 | 2020 年 7 月 31 日 |

逐行追溯表保存行情代码、统一证券身份、财务连接代码、市场、市值、当日交易状态、停牌标记、上市日期可用性、实际采用的报告期、公告时间、版本时间、TTM 归母净利润、原始 EP/PE、MAD 处理值和 Z 标准化值。历史 ST 和官方上市日期没有源表，相关可用性及“不筛选”状态分别记录在追溯表和 `point_in_time_field_audit.csv` 中。

### 2.3 描述性统计

| 统计量 | EP | PE |
|---|---:|---:|
| 有效观测数 | 6,732,630 | 6,732,630 |
| 均值 | 0.006925 | -13.3974 |
| 标准差 | 0.174645 | 12,684.6366 |
| 1% 分位数 | -0.416115 | -592.1720 |
| 中位数 | 0.021148 | 26.2719 |
| 99% 分位数 | 0.174537 | 751.4696 |
| 负值占比 | 21.36% | 21.36% |
| MAD 截断占比 | 11.07% | 14.16% |

最新截面为 2025 年 12 月 31 日，共有 5,438 条有效观测；EP 中位数为 0.01315，PE 中位数为 28.18 倍，负 EP/PE 占比为 28.26%。经过逐日 MAD 去极值和 Z 标准化后，EP 和 PE 各保留 6,732,629 条有效结果。

全样本中，PE 的最小值和最大值分别约为 `-3.73 x 10^6` 倍和 `5.39 x 10^5` 倍，说明 EP 接近零时倒数变换会形成明显长尾。因此，PE 更适合使用中位数、分位数或去极值结果进行解释，EP 则更适合直接进行横截面比较。

### 2.4 分行业原始 PE

申万一级行业分析使用 2025 年 12 月 31 日最新截面，按行业汇总未经 MAD 去极值和 Z 标准化的原始 PE。为避免接近零的 EP 使 PE 均值被极端值主导，柱高采用行业内原始 PE 的中位数。亏损公司的负 PE 仍保留在统计中。

行业中位 PE 最高的三个行业分别为通信（57.55 倍）、综合（53.22 倍）和国防军工（52.75 倍）。房地产的行业中位 PE 为 -1.89 倍，其负 PE 比例为 62.00%；负 PE 表示行业内亏损观测较多，不应解释为负估值倍数。图中红色虚线表示全市场原始 PE 中位数 28.18 倍。

![2025 年 12 月 31 日申万一级行业原始 PE 中位数](./图形/industry_pe_latest.png)

*图 1：2025 年 12 月 31 日申万一级行业原始 PE 中位数。样本包含 5,438 条 PE 和行业分类均有效的股票记录；红色虚线为全市场原始 PE 中位数 28.18 倍，红色柱表示行业中位 PE 为负。*

---

## 三、EP 横截面 RLM 回归

### 3.1 横截面线性模型

在每个因子形成日 `t`，设横截面内共有 `N` 只股票和 `K` 个行业。下一持有期收益写成一个 `N x 1` 列向量：

```text
Y_t = [r_1,t+1, r_2,t+1, ..., r_N,t+1]'
```

EP 因子暴露和市值暴露也分别写成 `N x 1` 列向量：

```text
x_t = [x_1,t, x_2,t, ..., x_N,t]'
m_t = [m_1,t, m_2,t, ..., m_N,t]'
```

其中，`x_i,t` 是股票 `i` 在形成日的标准化 EP，`m_i,t` 是标准化后的对数总市值。行业归属可写成一个 `N x K` 的哑变量矩阵。例如，4 只股票属于 3 个行业时：

```text
          行业1  行业2  行业3
股票1      1      0      0
股票2      0      1      0
股票3      0      0      1
股票4      1      0      0
```

每只股票只属于一个一级行业，因此每一行只有一个元素为 1。为了同时估计截距并避免行业哑变量陷阱，回归只放入 `K-1` 个行业哑变量。设计矩阵定义为：

```text
D_t = [1_N, x_t, m_t, I_1,t, ..., I_K-1,t]
```

`D_t` 的维度为 `N x (K+2)`，待估参数向量为：

```text
theta_t = [alpha_t, beta_EP,t, beta_Size,t,
           gamma_1,t, ..., gamma_K-1,t]'
```

所以当月的横截面模型为：

```text
Y_t = D_t * theta_t + epsilon_t

r_i,t+1 = alpha_t
          + beta_EP,t * x_i,t
          + beta_Size,t * m_i,t
          + sum(k=1,...,K-1){gamma_k,t * I_i,k,t}
          + epsilon_i,t+1
```

这里的系数在同一个月内对所有股票共同估计，所以应写成 `alpha_t`、`beta_EP,t` 和 `gamma_k,t`，而不是 `alpha_i` 或 `beta_i`。其中 `beta_EP,t` 就是该月 EP 暴露对应的横截面因子收益。

### 3.2 OLS 的估计与统计推断

OLS 选择使残差平方和最小的参数：

```text
S(theta_t)
  = sum(i=1,...,N){(y_i - d_i' * theta_t)^2}
  = (Y_t - D_t * theta_t)' * (Y_t - D_t * theta_t)
```

对 `theta_t` 求一阶导数并令其为零：

```text
dS / dtheta_t = -2 * D_t' * (Y_t - D_t * theta_t) = 0
```

得到正规方程：

```text
D_t' * D_t * theta_hat_t = D_t' * Y_t
```

当 `D_t` 满列秩时，可以写成：

```text
theta_hat_t = (D_t' * D_t)^(-1) * D_t' * Y_t
```

如果同时加入截距和全部 `K` 个行业哑变量，则行业哑变量之和恒等于截距列，`D_t` 不满秩，`D_t' * D_t` 没有逆。因此本次使用“截距 + `K-1` 个行业哑变量”，并在每个月拟合前检查设计矩阵的秩。实际数值计算也不应显式求逆，通常使用 QR、SVD 或线性最小二乘求解器提高稳定性。

设参数总数为 `p=K+2`，OLS 残差为：

```text
epsilon_hat_t = Y_t - D_t * theta_hat_t
```

在误差同方差且相互独立等经典假设即认为暴露市值行业后不同股票波动相同，且每只股票的波动是独立的不会影响其它股票，这个假设显然是有问题的，但是这里只是为了测试因子方便后续的t值标准误等计算，残差方差估计为：

```text
sigma_hat^2 = epsilon_hat_t' * epsilon_hat_t / (N-p)模型整体的拟合误差
```

系数协方差矩阵为（此处选择最简单的独立同分布假设作为基础计算，并不意味着这是正确的结果，相反，这个假设明显违背真实的情形，结果只是一个数据基准判断，后续可以修改协方差的估计）：

```text
Var(theta_hat_t) = sigma_hat^2 * (D_t' * D_t)^(-1) （通过线性变换，beta 的协方差正比于残差方差。）
```

第 `j` 个系数的标准误和 `t` 值为：

```text
SE(theta_hat_j,t) = sqrt(Var(theta_hat_t)[j,j])
t_j,t = theta_hat_j,t / SE(theta_hat_j,t)
```
标准误，即估计出来的线性方程与原始散点的方差，衡量估计的回归系数准不准，误差到底有多大
t值即衡量计算出来的beta和0的距离有几个标准误。如果我认为计算出来的beta是一个统计平均值，那么根据中心极限定理beta统计值应该服从一个正态分布，所以t值就是衡量这次统计的结果距离0有几个方差，出现这种情况的概率是多大。此处采用了同方差独立的假设，即认为当因子解释了收益之后剩下的残差是相互独立且方差相同的，但是这个假设是明显不符合真实情形的，及时中性化后不同行业，市值的波动也不同，所以这里用该假设计算的结果只是一个分析的基准，方便数据的计算，后续再这个基准上继续修改，而并不是认为这个结果是真实的。
大样本下常用的双侧判断标准为：

| 显著性水平 | 临界值（近似） |
|---|---:|
| 10% | `abs(t) > 1.65` |
| 5% | `abs(t) > 1.96` |
| 1% | `abs(t) > 2.58` |

精确的 OLS 双侧 `p` 值和置信区间使用自由度为 `N-p` 的 Student `t` 分布：

```tex
p_j = 2 * P(T_N-p > abs(t_j))

95% CI = theta_hat_j
         +/- t_(0.975, N-p) * SE(theta_hat_j)
```

### 3.3 为什么主回归使用 RLM

ols用最简单的平方和作为目标函数用样本估计真实值，但是如果我们认为真实的收益是服从正态分布的，然而统计量的收益总是具有尖峰厚尾的特征，此时偏离平均的极端值远大于正态分布估计的概率（为什么用mad去极值）极端值用平方估计对目标函数的贡献可能会远远放大，所以对于极端值我们既不能舍去也不能用平方放大影响，这里使用rlm回归，核心逻辑就是但对于极端值采取线性的贡献。（尖峰厚尾对于正态估计明显有问题，对于1，2阶矩的估计影响不大，高阶矩大，用t估计具有尖峰厚尾的性质）
![2025 年 12 月 29 日股票横截面单日收益率分布](./图形/daily_return_distribution_20251229.png)
主回归使用 RLM 对异常残差连续降权，尽量保留横截面信息。RLM 的目标函数为：

```text
min over theta_t:
sum(i=1,...,N){rho((y_i - d_i' * theta_t) / s_t)}
```

其中 `s_t` 是使用 MAD 估计的当月稳健残差尺度。本次采用 Huber 损失，调节参数 `c=1.345`：

```text
rho(u) = 0.5 * u^2,                    abs(u) <= c
rho(u) = c * abs(u) - 0.5 * c^2,       abs(u) > c
```
此处c选择=1.345，原因，就是说如果我的数据完全是服从正态分布的，我选择对1.345外的降权重，计算结果比真实值大5%，实际覆盖未被降权重的真实样本占据85%左右，并且抗异常效果强。1.345只是一个选择的值综合抗异常，有效率和覆盖样本。
小残差仍按平方损失处理；大残差改为近似线性增长，因此不会像 OLS 那样让极端值的影响按平方增加。对应的迭代权重为：

```text
w_i = 1,                               abs(u_i) <= c
w_i = c / abs(u_i),                    abs(u_i) > c
```

RLM 使用迭代加权最小二乘（IRLS）求解，具体过程如下。

1. **使用 OLS 得到初始系数。** 第一次计算时所有股票的权重均为 1：

   ```text
   theta^(0) = (D_t' * D_t)^(-1) * D_t' * Y_t
   ```

2. **根据初始系数计算残差和稳健尺度。**

   ```text
   e^(0) = Y_t - D_t * theta^(0)

   s^(0)
     = median_i(abs(e_i^(0) - median(e^(0)))) / 0.6745
   ```

   除以 `0.6745` 是为了使 MAD 在正态分布下与标准差处于相同尺度。在每轮得到新残差后重新估计。

3. **在第 `q` 轮计算标准化残差。**

   ```text
   u_i^(q) = e_i^(q) / s^(q)
   ```

4. **根据 Huber 规则更新每只股票的稳健权重。**

   ```text
   w_i^(q) = 1,                         abs(u_i^(q)) <= 1.345
   w_i^(q) = 1.345 / abs(u_i^(q)),      abs(u_i^(q)) > 1.345

   W^(q) = diag(w_1^(q), ..., w_N^(q))
   ```

   小残差股票继续获得完整权重；残差越大，下一轮中的权重越低，但观测不会被直接删除。

5. **使用本轮权重进行一次加权最小二乘，得到下一轮系数。**

   ```text
   theta^(q+1)
     = (D_t' * W^(q) * D_t)^(-1)
       * D_t' * W^(q) * Y_t
   ```

6. **重新计算残差和 MAD 尺度。**

   ```text
   e^(q+1) = Y_t - D_t * theta^(q+1)

   s^(q+1)
     = median_i(abs(e_i^(q+1) - median(e^(q+1)))) / 0.6745
   ```

   然后使用新的 `e^(q+1)` 和 `s^(q+1)` 返回第 3 步，继续计算下一组权重和系数。

7. 判断是否终止。以系数变化作为收敛条件：

   ```text
   max_j(abs(theta_j^(q+1) - theta_j^(q))) <= 1e-8
   ```

   满足该条件时停止迭代；如果尚未满足，则最多迭代 100 次。本次 64 个月度模型均在达到上限之前收敛。

 
本次使用 `HuberT(c=1.345)`、MAD 尺度、H1 稳健协方差、最多 100 次迭代和 `1e-8` 收敛精度。迭代收敛后，H1 不再像 OLS 那样直接用残差平方和估计一个共同方差，而是用 Huber 得分函数 `psi(u)` 及其导数修正协方差：

```text
u_i = e_i / s

Cov_H1(theta_hat)
  = k^2
    * { [sum psi(u_i)^2 / (N-p)] * s^2
        / [mean psi'(u_i)]^2 }
    * (D_t' * D_t)^(-1)

k = 1 + (p/N) * Var(psi'(u_i)) / mean(psi'(u_i))^2
SE_H1(theta_hat_j) = sqrt(Cov_H1(theta_hat)[j,j])
z_H1,j = theta_hat_j / SE_H1(theta_hat_j)
```

因此，单月系数除以 H1 标准误得到的是大样本近似 `z` 值，而不是经典同方差 OLS 的有限样本 `t` 值。H1 只处理当月横截面参数的不确定性，不能直接估计 64 个月系数均值的标准误。为了先跑通流程，本次暂时假设月度系数相互独立且同方差，用普通均值标准误进行全期和年度汇总：

```text
beta_bar_EP = sum(t=1,...,M){beta_EP,t} / M
SE_IID = sd(beta_EP,t) / sqrt(M)
t_IID = beta_bar_EP / SE_IID
p_IID = 2 * P(T_(M-1) > abs(t_IID))
```

这里没有执行 Newey-West HAC；`t_IID` 只是当前独立同方差假设下的基准统计量。

#### 一日横截面收益率分布示例

为直观展示 RLM 所处理的异常收益问题，选取 2025 年 12 月 29 日，对当日股票计算未经截尾或去极值的复权收益率：

```text
daily_return[i,t] = close_adj[i,t] / pre_close_adj[i,t] - 1
```

该日原始行情有 5,545 条记录，按研究口径排除 79 条沪深 B 股记录和 11 条停牌记录后，有效横截面包含 5,455 只股票。选择该日是为了使用接近样本末端、且没有被上市首日数倍涨幅主导的完整交易日，日期选择不会改变正式回归样本。

| 统计量 | 实际分布 | 同均值、同标准差正态分布 |
|---|---:|---:|
| 均值 | -0.2172% | -0.2172% |
| 中位数 | -0.4878% | -0.2172% |
| 标准差 | 2.8354% | 2.8354% |
| 偏度 | 1.1009 | 0 |
| 超额峰度 | 12.8128 | 0 |
| 均值正负 0.5 个标准差以内 | 61.72% | 38.29% |
| 均值正负 3 个标准差以外 | 2.60%（142 只） | 0.27%（约 15 只） |

 均值，方差，偏度，峰度，分别是一个分布的一阶到四阶矩，偏度小于0，即数据集中于均值左边反之。峰度描述的是尾部厚度，峰度越大，尾巴越厚峰越尖。
### 3.4 固定测试口径

- 因子形成日：每个月最后一个交易日；
- 建仓日：下个月第一个交易日，以 `close_adj` 复权收盘价建仓；
- 退出日：下个月第一个交易日，以 `close_adj` 复权收盘价退出；
- 因子：当日经过 MAD 去极值和 Z 标准化的 EP；
- 控制变量：形成日横截面标准化后的对数总市值，以及 `K-1` 个申万一级行业哑变量；
- 样本不筛除 ST/PT 或次新股，固定成交端点停牌或价格缺失时前瞻收益记为缺失。

测试模型为：

```text
r_fwd[i,m+1] = alpha[m] + beta_EP[m] * EP_Z[i,m]
                 + beta_Size[m] * Size_Z[i,m]
                 + K-1 个申万一级行业哑变量 + epsilon[i,m+1]
```

RLM 使用 `HuberT(c=1.345)`、MAD 残差尺度和 H1 协方差，单月系数使用 H1 标准误计算大样本 `z` 值。月度系数序列的全期和年度汇总暂按独立同方差假设计算普通均值 `t` 检验，不执行 Newey-West HAC。

### 3.5 样本与拟合情况

共得到 64 个完整持有期，因子形成日从 2020 年 7 月 31 日至 2025 年 10 月 31 日，首次建仓日为 2020 年 8 月 3 日，最后退出日为 2025 年 12 月 1 日。控制变量和行业均有效的观测为 313,200 条，其中 312,177 条具有有效固定端点收益。单月最终样本数介于 3,812 和 5,400 之间，中位数为 5,011。


### 3.6 全期结果

| 指标 | 结果 |
|---|---:|
| 回归月份 | 64 |
| EP 月均系数 | 0.1623% |
| EP 系数中位数 | 0.1465% |
| EP 系数标准差 | 0.8964% |
| EP IID 标准误 | 0.1120% |
| EP IID `t` 值 | 1.4487 |
| EP IID `p` 值 | 0.1524 |
| EP 系数为正的月份 | 53.12% |
| 单月 `abs(z) >= 2` 的比例 | 82.81% |
| 正向且 `z >= 2` 的比例 | 43.75% |
| 负向且 `z <= -2` 的比例 | 39.06% |
| Size 月均系数 | -0.4540% |
| Size IID `t` 值 | -2.1758 |
| 平均 RLM 降权比例 | 23.56% |
| 平均伪 R² | 12.22% |

EP 已按形成日横截面标准化，因此月均系数 0.1623% 表示 EP 提高一个横截面标准差时，下一持有期收益平均提高约 0.1623%。简单乘以 12 后约为 1.95%，但这只是因子溢价尺度，不是可交易组合年化收益。

### 3.7 分年度结果

| 年份 | 月数 | EP 月均系数 | IID `t` 值 | IID `p` 值 | 正系数比例 |
|---|---:|---:|---:|---:|---:|
| 2020 | 6 | 0.2476% | 1.2853 | 0.2550 | 66.67% |
| 2021 | 12 | -0.0067% | -0.0274 | 0.9787 | 58.33% |
| 2022 | 12 | -0.0323% | -0.1801 | 0.8603 | 41.67% |
| 2023 | 12 | 0.3872% | 1.7288 | 0.1118 | 75.00% |
| 2024 | 12 | 0.4144% | 1.0109 | 0.3338 | 50.00% |
| 2025 | 10 | -0.0249% | -0.0970 | 0.9248 | 30.00% |

### 3.8 结果解释

EP 的全期平均系数方向符合预期，但在月度系数独立同方差假设下 `p=0.1524`，未达到常用的 5% 显著性标准。单月 H1 `z` 值显著的比例较高，而全期均值不显著，原因是显著月份中正向占 43.75%、负向占 39.06%，方向抵消明显。年度结果也不稳定：2023—2024 年为正，2021—2022 年及 2025 年接近零或为负。因此，当前 RLM 结果只能说明 EP 存在正向平均溢价迹象，尚不能单独认定其具有稳定选股能力，还需要结合 Rank IC、行业内分层回测以及 OLS/WLS 对照。

![RLM 月度 EP 系数与累计因子溢价](./回归结果/ep/RLM_H1_独立同方差/rlm_factor_premium.png)

完整逐月结果、样本审计和回归权重见 [`回归结果/ep/RLM_H1_独立同方差/RLM统计结果.md`](./回归结果/ep/RLM_H1_独立同方差/RLM统计结果.md)。
