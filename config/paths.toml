# 基础数据目录的仓库默认值。
#
# 解析优先级（从高到低）：
#   1. 环境变量 EP_FACTOR_DATA_ROOT
#   2. config/paths.local.toml   （本机覆盖，不纳入版本管理）
#   3. 本文件
#
# 克隆仓库后请复制本文件为 paths.local.toml，并改成自己机器上的数据目录，
# 不要直接修改本文件，避免把本机路径提交上去。

data_root = "D:/实习生学习项目/基础数据"
