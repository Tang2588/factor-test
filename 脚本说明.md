# 脚本说明

本文档逐条说明 `代码/` 下每个脚本的作用、输入、输出和运行方式。
目录结构与口径定义见 [README.md](README.md)，本次共享模块抽取的过程见
[日志/20260921_共享模块抽取与口径统一.md](日志/20260921_共享模块抽取与口径统一.md)。

## 一、依赖顺序

```
                    基础数据/chn_equ_mkt_quotation.parquet
                                  │
                    ┌─────────────┴─────────────┐
                    ▼                           ▼
        step1_样本筛选                 vw_fdmt_is_new.parquet
        （行情清洗 + B股剔除 + 北交所映射）        │
                    │                           ▼
                    │                  step2_利润表处理
                    │                           │
                    │                           ▼
                    │                  step3_TTM计算
                    │                           │
                    └─────────────┬─────────────┘
                                  ▼
                    step4_点时间对齐_计算PE
                    （原始 EP / PE，点时口径）
                                  │
                                  ▼
                    step5_MAD去极值_Z标准化
                    （正式因子结果）
                                  │
                    ┌─────────────┴─────────────┐
                    ▼                           ▼
    cross_section_rlm / cross_section_ols   analyze_mad_standardization
            （通用横截面回归）                  （标准化统计与图形）
                                  │
                                  ▼
                        factor_verify（交付验收）
```

除 `代码/common/` 外，每个脚本都可独立运行，按上图顺序执行即可。二次运行时，
`step1`–`step3` 只在数据源更新后需要重跑。

## 二、共享底层 `代码/common/`

不含 `main()`，不写任何结果文件，只提供函数供其他脚本调用。所有路径与常量定义
在 `paths.py`，数据源目录按 `EP_FACTOR_DATA_ROOT` → `config/paths.local.toml` →
`config/paths.toml` 的顺序解析。

| 文件 | 提供的主要函数 | 说明 |
|---|---|---|
| `paths.py` | `PROJECT_ROOT`、`DATA_ROOT`、`MARKET_PATH`、`EP_PATH` 等 | 路径与回归共享常量（`HUBER_T`、`MAX_ITER`、`TOL`、`MIN_OBSERVATIONS`） |
| `parquet_io.py` | `read_selected_dates`、`read_all_dates`、`scan_market_index` | 分片读取；一次扫描同时返回交易日历与北交所代码切换位置 |
| `market.py` | `prepare_market_snapshot`、`derive_bse_mapping`、`apply_security_id` | 行情清洗、B 股剔除、北交所新旧代码映射与校验 |
| `calendar.py` | `build_monthly_calendar` | 生成形成日／建仓日／退出日 |
| `returns.py` | `build_endpoint_forward_returns` | 端点口径前瞻收益，逐条记录缺失原因 |
| `panel.py` | `prepare_formation_panel`、`prepare_industry`、`build_regression_panel` | 形成日面板、行业面板、回归样本与规模 Z 值 |

调用方式（在 `代码/` 下的脚本中）：

```python
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent))

from common import MARKET_PATH, read_selected_dates, build_monthly_calendar
```

## 三、数据清洗与因子计算

### `step1_样本筛选.py`

| 项目 | 内容 |
|---|---|
| 作用 | 构建沪深 A 股 + 北交所行情面板；剔除 B 股；识别北交所新旧代码映射；标记次新股、停牌、上市日期可得性；打点时字段审计 |
| 输入 | `基础数据/chn_equ_mkt_quotation.parquet` |
| 输出 | `中间结果/_mkt_clean.parquet`、`bse_code_mapping.parquet/.csv`、`b_share_codes.parquet`、`point_in_time_field_audit.csv/.parquet` |
| 运行 | `python "代码\step1_样本筛选.py"` |
| 备注 | 剔除 50 只沪市 B 股（900xxx.BJ）与 39 只深市 B 股（200/201xxx.SZ），数量不符会直接报错。北交所映射调用 `common.market.derive_bse_mapping` |

### `step2_利润表处理.py`

| 项目 | 内容 |
|---|---|
| 作用 | 保留利润表的点时间版本，按公告时间确定版本顺序，处理北交所新代码到旧财务代码的映射 |
| 输入 | `基础数据/vw_fdmt_is_new.parquet`、`中间结果/bse_code_mapping.parquet` |
| 输出 | `中间结果/reports.parquet` |
| 运行 | `python "代码\step2_利润表处理.py"` |
| 备注 | 必须先在 `step1` 生成北交所映射 |

### `step3_TTM计算.py`

| 项目 | 内容 |
|---|---|
| 作用 | 按累计口径四种情形计算点时间 TTM 归母净利润，并记录每个观测的取值来源 |
| 输入 | `中间结果/reports.parquet` |
| 输出 | `中间结果/reports_ttm.parquet`、`reports_effective.parquet`、`report_version_decisions.parquet` |
| 运行 | `python "代码\step3_TTM计算.py"` |
| 备注 | `report_version_decisions.parquet` 是旧公告不得覆盖新公告的审计记录 |

### `step4_点时间对齐_计算PE.py`

| 项目 | 内容 |
|---|---|
| 作用 | 把 TTM 净利润按公告时间对齐到行情日，计算原始 EP 与 PE，并逐行记录所用财务观测 |
| 输入 | `中间结果/_mkt_clean.parquet`、`中间结果/reports_ttm.parquet` |
| 输出 | `中间结果/ep_raw.parquet`、`pe_raw.parquet`、`factor_audit.parquet` |
| 运行 | `python "代码\step4_点时间对齐_计算PE.py"` |
| 备注 | 行情日 T 只用截至当日已可获得的财务信息，不允许未来数据；原始 EP/PE 的精确定义是 `EP = TTM 归母净利润 / 总市值`、`PE = 1 / EP` |

### `step5_MAD去极值_Z标准化.py`

| 项目 | 内容 |
|---|---|
| 作用 | 逐日横截面 MAD 去极值（3 倍 MAD）与 Z 标准化，EP 与 PE 独立处理；生成覆盖率与测试起点 |
| 输入 | `中间结果/ep_raw.parquet`、`pe_raw.parquet`、`factor_audit.parquet` |
| 输出 | `因子结果/ep.parquet`、`pe.parquet`；`中间结果/ep_mad.parquet`、`pe_mad.parquet`、`ep_processing_stats.parquet`、`pe_processing_stats.parquet`、`factor_daily_coverage.csv/.parquet`、`factor_test_start.json` |
| 运行 | `python "代码\step5_MAD去极值_Z标准化.py"` |
| 备注 | `factor_test_start.json` 里的 `formal_test_start_date` 是覆盖率爬坡结束后的正式测试起点，回归脚本会读取它 |

## 四、横截面回归

这两个脚本与具体因子无关：**输入一份因子文件，输出该因子的回归统计结果**。
换成其它因子只需改命令行参数，不需要改代码。因子文件格式与交付一致：
`index = ['date', 'stock_code']`，`columns = ['signal']`。

两者共用 `代码/common/`，因此样本、前瞻收益、控制变量、行业哑变量和月度日历
完全一致，结果差异只来自估计方法。

### `cross_section_rlm.py`（主回归）

| 项目 | 内容 |
|---|---|
| 作用 | 月度横截面 Huber 稳健回归，模型为 `r = α + β_F·F_Z + β_Size·Size_Z + 行业哑变量 + ε`，其中 F 为输入因子 |
| 输入 | 因子文件、行情与行业基础数据、`中间结果/factor_test_start.json` |
| 输出 | `回归结果/<因子名>/RLM_H1_独立同方差/`，含逐月系数、年度与全期汇总、逐股样本与稳健权重、审计表、图表与统计报告 |
| 运行 | `python "代码\cross_section_rlm.py"`（实测约 23 秒） |
| 设定 | `HuberT(c=1.345)`、MAD 残差尺度、H1 稳健协方差、最多 100 次迭代、1e-8 收敛；时间序列推断为月度系数的独立同方差 t 检验，未启用 Newey-West HAC |

命令行参数：

| 参数 | 默认值 | 说明 |
|---|---|---|
| `--factor` | `ep` | 因子名或因子文件路径。因子名解析为 `因子结果/<名称>.parquet`；带 `.parquet` 或已存在的路径按路径处理 |
| `--factor-name` | 取因子文件名 | 报告标题与配置里显示的因子标签 |
| `--out-root` | `回归结果/` | 结果根目录，最终写入 `<out-root>/<因子名>/RLM_H1_独立同方差/` |
| `--min-observations` | `500` | 单月最小观测数 |
| `--huber-t` | `1.345` | Huber 调优常数 |

```powershell
python "代码\cross_section_rlm.py"                                     # 因子结果/ep.parquet
python "代码\cross_section_rlm.py" --factor pb                         # 因子结果/pb.parquet
python "代码\cross_section_rlm.py" --factor "D:/其它/my.parquet" --factor-name MY
python "代码\cross_section_rlm.py" --factor "D:/因子计算/规模因子_交付版/因子结果/size.parquet"
```

已实测：用默认参数跑 `ep` 与用路径跑规模因子 `size.parquet` 都能直接出结果，
规模因子的全期月均系数为 −1.478%、IID t 值 −3.834，输出落在
`回归结果/size/RLM_H1_独立同方差/`。

### `cross_section_ols.py`（同口径对照）

| 项目 | 内容 |
|---|---|
| 作用 | 与 RLM 完全相同的样本与设计矩阵，改用最小二乘和经典同方差标准误 |
| 输入 | 同 `cross_section_rlm.py` |
| 输出 | `回归结果/<因子名>/OLS_月度/` |
| 运行 | `python "代码\cross_section_ols.py"`（实测约 13 秒） |
| 参数 | `--factor`、`--factor-name`、`--out-root`、`--min-observations`，含义同上 |
| 备注 | 旧版「每月第一个交易日调仓 + 日收益复合」口径的输出已归档到 `回归结果/_历史口径存档/OLS_日复合收益/`，不再使用 |

## 五、分析与绘图

| 脚本 | 作用 | 输入 | 输出 |
|---|---|---|---|
| `analyze_mad_standardization.py` | MAD 去极值与 Z 标准化的完整统计分析 | `中间结果/factor_audit.parquet`、各 `*_processing_stats.parquet`、行业基础数据 | `标准化统计/` 下 10 张统计表、3 张图与统计报告 |
| `plot_ep_pe分布.py` | 原始 EP / PE 分布直方图 | `中间结果/ep_raw.parquet`、`pe_raw.parquet` | `图形/` |
| `plot_industry_pe.py` | 最新截面分行业 PE 柱状图 | `中间结果/pe_raw.parquet`、行业基础数据 | `图形/industry_pe_latest.png`、`中间结果/industry_pe_latest.csv`、`图形/行业PE柱状图.tex` |
| `plot_daily_return_distribution.py` | 单日收益分布（尖峰厚尾特征） | 行情基础数据 | `图形/daily_return_distribution_<日期>.png`、`中间结果/daily_return_stats_<日期>.csv` |
| `render_markdown_pdf.py` | 把 Markdown 统计文档渲染为 PDF | 指定 Markdown 文件 | 同名 PDF |

统一运行方式：`python "代码\<脚本名>.py"`。

## 六、验收

| 脚本 | 作用 | 说明 |
|---|---|---|
| `factor_verify.py` | 完整交付验收：因子索引与唯一性、覆盖率、MAD 处理统计、点时违规、B 股剔除、北交所映射、财务版本顺序 | 推荐使用，覆盖全链路 |
| `EP因子交付验证.py` | 早期精简版验收，只检查因子文件基本结构与 B 股剔除 | 保留作为历史记录 |

运行方式：`python "代码\factor_verify.py"`。

## 七、辅助模块

| 文件 | 说明 |
|---|---|
| `pure_factor_streaming.py` | 低内存流式计算助手，被 `step4` 与 `step5` 调用，负责分块读写与横截面标准化 |
| `bse_code_mapping.py` | 兼容层。映射算法已统一到 `common.market.derive_bse_mapping`，本文件只保留 `step1` 使用的旧入口，新代码请直接调用共享模块 |

## 八、已知待办

1. `plot_daily_return_distribution.py` 与 `plot_industry_pe.py` 仍把基础数据目录
   写死为 `D:\实习生学习项目\基础数据`，尚未接入 `common.paths` 的配置解析。
2. Rank IC 检验、行业内分层回测、交易成本与稳健性检验尚未实现。
3. 异常值处理的四版本对比（原始 / 宽松 MAD / asinh / 秩正态化）尚未落地。
4. Newey-West HAC 未启用。
5. **文档与 PDF 暂不同步（待 IC 测试完成后一并处理）。**
   `EP_PE统计分析与因子测试计划.md` 已在 2026-09-21 完成简化并把公式推导移入
   附录 A，但同名 `.tex` 与 `.pdf` 仍是简化前的版本，三者内容不一致。
   等 Rank IC 测试跑完、正文补充 IC 结果之后，再一次性重新生成 `.tex` 并编译
    PDF，避免重复编译。（`MAD去极值与标准化统计分析` 三件套已核对一致，不受影响。）
