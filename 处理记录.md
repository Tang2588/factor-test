
# 市盈率（PE → EP）因子计算 —— 处理记录

> **任务要求**
> - 输出：index = (date, stock_code)，columns = ['signal']
> - date 必须为交易日
> - T 日的因子值用于 T+1 日交易（点时间对齐，不允许使用开盘前不可获得的数据）
> - 记录从数据处理到因子计算的全流程，用数据案例说明处理方法

## 2026-09-14 交付更新（以本节和交付目录说明为准）

本次按新的交付要求重跑 EP/PE：

- 不筛选 ST/PT；次新股只保留标记，不再因为次新标记把因子置为 NaN；主动 NaN 掩码只使用行情表中的停牌日。
- 在行情表原始 `stock_code` 上排除同时满足 `900` 开头、`.BJ` 结尾的 B 股，共 50 只，名单见 `EP因子_交付版\中间结果\b_share_codes.parquet`。
- 同时生成原始 EP、原始 PE，以及分别经过 MAD/Z 标准化的 EP 和 PE；EP 原始中间结果为 `ep_raw.parquet`，PE 原始中间结果为 `pe_raw.parquet`。
- 本次清洗后行情 7,089,124 行；原始有效 EP/PE 各 6,719,132 条；停牌标记 22,022 行；点时未来数据违规 0。

后文 2026-08-19 的内容记录的是旧交付版本的历史过程；涉及样本过滤和输出文件时，以本更新为准。

---

## 重要口径变更（2026-08-19，依据当前代码）

| 项目 | 旧版 | 当前版 | 理由 |
|---|---|---|---|
| 因子定义 | PE = 总市值/TTM，亏损置 NaN | **EP = TTM归母净利润/总市值**，亏损保留负值 | PE 在亏损处不可定义、微利处爆炸（max 53.9 万）；EP 线性、对亏损股仍可排序，标准化更稳。盈利区间内 EP = 1/PE，信息不损失 |
| PIT 切点 | T 日零点（当日披露次日生效） | **T 日收盘后切点**（`date+1天−1ns`），T 日（含收盘后）披露即进 T 日行 | T 日行用于 T+1 日交易，T 日晚间披露的信息在 T+1 开盘前可知，符合任务约束且比旧口径快 1 天 |
| 利润表去重 | `groupby().first()` | `sort + drop_duplicates(keep='first')` | `.first()` 逐列取首个非空值，可能把「最早发布时间」与「后面行的数值」拼在一起（隐性 PIT 风险） |
| TTM 组件 | 不校验组件可得性 | 新增**组件发布时间 ≤ 本期报告**校验 | 本期报告披露时组件尚未入库/发布的，TTM 置 NaN |
| 停牌判定 | 仅 `suspended==1` | `suspended==1 ∪ status=='停牌' ∪ 两字段均缺失`，并保留 `suspended_unknown` 列 | 双字段交叉验证；缺失按停牌处理（保守） |
| 输出 | pe.parquet | **ep.parquet + pe.parquet** + `_mask.parquet`（新增 `suspended_unknown`、`st_filter_applied=0`、`st_filter_note`） | 掩码表留痕「ST 过滤未执行」等口径事实；原始 EP/PE 分别保存在 `ep_raw.parquet`、`pe_raw.parquet` |

旧版本运行：中间数据生成于 2026-08-18，`ep.parquet`/`_mask.parquet` 生成于 2026-08-19。2026-09-14 更新后的实际产物和统计以本文开头的交付更新为准。

---

## 数据源清单

| 文件 | 内容 | 本次用途 |
|---|---|---|
| `基础数据/chn_equ_mkt_quotation.parquet` | 日行情（715 万行×34 列，2020-01-02~2025-12-31，6016 股） | 总市值 me_total、停牌判断（`suspended` + `status` 双字段）、上市日期推断 |
| `基础数据/vw_fdmt_is_new.parquet` | 利润表（47 万行×95 列） | 归母净利润 N_INCOME_ATTR_P（EP 分子） |
| `基础数据/vw_fdmt_bs_new.parquet` | 资产负债表 | （EP 不用，PB/ROE 用） |
| `基础数据/chn_equ_info_calendar.parquet` | 交易日历（仅深市 XSHE） | 交易日参考 |
| `基础数据/chn_equ_indus_sw.parquet` | 申万行业分类 | 后续行业中性化用 |

**单位确认**：市值、净利润均为「元」（工商银行总市值 2.83 万亿、归母净利润 3659 亿，量级正确）。

**EP 公式**：`EP = TTM归母净利润 / 总市值(me_total)`（元/元，无量纲；盈利时等于 1/PE）

---

## 第 1 步：样本筛选（次新股、停牌均「标记 + 不删除」）

- 脚本：`代码/step1_样本筛选.py`
- 输出：`数据/_mkt_clean.parquet`（列：date, code6, me_total, suspended, suspended_unknown, is_cixin, listing_date_known, first_seen_date）

### 遇到的问题 → 处理方法

**问题 1：股票代码格式不一致**
- 现象：行情表 `stock_code` 是 `'601398.SH'`（带后缀），财务表是纯 6 位。
- 处理：正则 `(\d{6})` 提取 6 位代码作关联键；提取失败（NaN）直接报错退出，不带病运行。

**问题 2：面板唯一性**
- 处理：入口断言 `(date, code6)` 无重复，重复即报错（实测 0 重复）。

**问题 3：数据无 ST/PT 标记**
- 现象：34 列中无股票名称、无 ST 字段。
- 处理：不单独剔除 ST；在 `_mask.parquet` 写入 `st_filter_applied=0` 与 `st_filter_note='historical ST/PT data unavailable'` 留痕。亏损股由 EP 负值天然表达。

**问题 4：次新股上市日期被停牌日错位（关键 bug，已修复）**
- 现象：若先剔除停牌行再算上市日期，2020-01-02 当天停牌的老股票（如 600538）「首次出现日」被错推到复牌日，误判为次新股。
- 处理：**先用原始全量数据算 first_seen_date，再做任何筛选**。

**问题 5：数据起点边界**
- 现象：2020 年前上市的老股票，「首次出现日」= 数据起点 2020-01-02，不是真实上市日。
- 处理：`listing_date_known = first_seen_date > data_start`，只对「数据内才能确认上市的新股」判次新。

**问题 6：次新股阈值**
- 处理：上市不足 365 天（自然日）标 `is_cixin=1`，标记不删行，阈值可调。

**问题 7：停牌判定双字段交叉验证（新增）**
- 现象：`suspended` 数值字段与 `status` 文本字段并存，理论上可能不一致或同时缺失。
- 处理：`suspended = (suspended==1) ∪ (status=='停牌') ∪ (两字段均缺失)`，另存 `suspended_unknown` 供下游识别「不确定是否停牌」的行。
- 实测：`status` 取值为 上涨/下跌/平盘/涨停/跌停/一字涨停/一字跌停/停牌（NaN 5,508 行但 suspended 字段有值）；`status=='停牌'` 与 `suspended==1` **完全一致，互相印证，无新增行**；`suspended_unknown=0` 行。即新口径下停牌行数与旧口径相同。

**问题 8：停牌日的数据其实是「冻结值」**
- 现象：停牌日 `me_total` 被前值填充（冻结在最后一笔成交价），`ret` 恒 0。例：`000670` 停牌 589 个交易日，`me_total` 一直卡在 1.837e9。
- 处理：**标记不删除**（方案 B）。停牌是「交易池」问题，应在回测用 T+1 是否停牌过滤；因子侧只置 NaN。复牌日（如盈方微复牌日市值单日 +488%）用新市值重新计算，标记方案天然正确。

**问题 9：超长停牌股**
- 现象：停牌段中位数 2 天，但最长 589 天（约 2.4 年），均为重组/破产重整/退市类（000670、002260、000792、002450 等）。
- 处理：同问题 8，标记 + NaN。

**次新 ≠ 停牌，标记必须分开**：停牌 = 不可交易，EP/PE 置 NaN；次新只记录 `is_cixin`，不主动置空因子。两列均保留在 `_mask.parquet` 供下游区分。

### 处理结果

| 项目 | 数值 |
|---|---|
| 原始行情 | 7,155,034 行（全部保留） |
| 停牌标记（suspended=1） | 22,675 行 |
| 次新标记（is_cixin=1） | 473,286 行，涉及 **2,206 只**（= 数据内可确认上市日的全部新股，因上市首日必在 365 天窗口内） |
| 停牌 ∩ 次新 | 217 行（两标记独立共存） |
| me_total 缺失 | 5 行（后续 EP 自然为 NaN） |

### 数据案例

1. **停牌案例**：`600538` 于 2020-01-02 停牌、01-06 复牌——若先删停牌行，上市日会被错判为 01-06。
2. **次新案例**：`688181` 2020-01-06 上市，其后 365 天内标 `is_cixin=1`。

---

## 第 2 步：利润表处理

- 脚本：`代码/step2_利润表处理.py`
- 输出：`数据/reports.parquet`（列：code6, END_DATE, FISCAL_PERIOD, ACT_PUBTIME, N_INCOME_ATTR_P）
- 目标：取出一份「点时间正确」的累计归母净利润序列。

### 遇到的问题 → 处理方法

**问题 1：关键字段缺失行**
- 处理：code6 无法解析、发布时间/报告期/口径/净利润为空的行先行剔除（10,797 行）。这些行无法参与点时间对齐，混入只会污染。

**问题 2：合并报表 vs 母公司报表（MERGED_FLAG）**
- 现象：`'1'`（合并）与 `'2'`（母公司）并存，同一报告期两条记录。
- 处理：只保留 `MERGED_FLAG == '1'`（按字符串比较前先 strip，兼容 '1'/' 1'/1）。

**问题 3：单季口径 vs 累计口径（FISCAL_PERIOD）**
- 现象：例：平安银行 2019-09-30 同时有 `FISCAL_PERIOD=3`（单季 8.218e9）与 `9.0`（前三季度累计 2.3621e10）。
- 处理：`END_DATE 月份 == FISCAL_PERIOD` 只留累计口径（TTM 公式需要）。

**问题 4：重述/比较期重复行**
- 现象：同一 `(股票, END_DATE)` 多行，`ACT_PUBTIME` 相差约一年（原始发布 vs 作为次年报告的比较期重新录入），152,477 个报告期中约 10.5 万个存在重复。
- 处理：按 `(code6, END_DATE, ACT_PUBTIME)` 排序后 `drop_duplicates(keep='first')`，每个报告期只保留**最早发布的那一整行**——首次可得即用，避免未来数据泄露。若重述值不同，取最早原始值（更正公告的影响属于已知设计取舍，见文末「遗留事项」）。

**问题 5：`groupby().first()` 的逐列拼凑隐患（本次修复）**
- 现象：旧写法 `groupby(...).first()` 的语义是**逐列取首个非空值**，不是取第一整行。若最早行的净利润为空，会从后面行取数值、却配上最早行的发布时间——形成 lookahead 校验查不出来的隐性 PIT 违规。
- 处理：改为 `drop_duplicates(keep='first')` 整行去重。旧口径下本数据实测 0 例拼凑（最早行净利润从不为空），但隐患已从写法上消除；另有关键字段缺失行先行剔除，避免旧法为「全组行都缺字段」的 3,657 个报告期拼出实际不存在的记录。

**问题 6：ACT_PUBTIME 与 PUBLISH_DATE**
- 处理：点时间一律用 **ACT_PUBTIME**（精确到秒的披露时刻，如 `2020-02-13 19:02:36`）。

**问题 7：数据覆盖起点（点时间地平线）**
- 现象：`ACT_PUBTIME` 最早 2020-01-01，更早发布的报告只作为比较期数据在 2020 年后入库。
- 处理：不回填。2020 年初大部分股票无可用财报、EP 为 NaN，属预期（详见第 5 步爬坡表）。

### 处理结果

| 步骤 | 剩余行数 | 剔除行数 |
|---|---|---|
| 原始利润表 | 472,983 | — |
| 关键字段完整 | 462,186 | 10,797 |
| 只保留合并报表 | 276,553 | 185,633 |
| 只保留累计口径 | 266,866 | 9,687 |
| 同一报告期取最早发布 | **148,820** | 118,046 |

覆盖 6,461 只股票；END_DATE 月份分布：3 月 35,260 / 6 月 36,004 / 9 月 36,300 / 12 月 41,256，全部为报告期末（月末），无月度报告混入。

### 数据案例

1. **单季/累计**：`000001` 平安银行 2019-09-30——单季 8.218e9、累计 2.3621e10，保留累计（验证：2.3621e10 − 8.218e9 = 1.5403e10 = 上半年累计，一致）。
2. **重述**：`000001` 2019 年报有 `2020-02-13`（原始）与 `2021-02-01`（比较期）两行，值相同，保留 2020-02-13。
3. **时间戳**：`000001` 2019 年报 `ACT_PUBTIME=2020-02-13 19:02:36`（披露日晚间）、`PUBLISH_DATE=2020-02-14`，点时间用前者。

---

## 第 3 步：TTM 归母净利润计算

- 脚本：`代码/step3_TTM计算.py`
- 输出：`数据/reports_ttm.parquet`（列：code6, END_DATE, ACT_PUBTIME, TTM_ni）

### 公式（累计口径 4-case，END_DATE.month = P）

- P=12（年报）：`TTM = NI(本期)`
- P=3/6/9：`TTM = NI(本期累计) + NI(去年年报) − NI(去年同期累计)`

实现：用两次 left merge（`validate=many_to_one / one_to_one`）把「去年年报」「去年同期累计」两个组件拼到每条报告上，并**同时取组件各自的发布时间**。

### 遇到的问题 → 处理方法

**问题 1：去年同期组件缺失 → TTM 为 NaN**
- 现象：数据库点时间覆盖起点在 2020 年，更早的季度数据只有作为比较期入库的版本。
- 处理：组件缺失（17,783 条季度报告）即置 NaN，不回填。

**问题 2：组件发布时间晚于本期报告（新增校验）**
- 现象：本期报告披露时，「去年年报」在库内的最早发布时间可能晚于本期报告——即披露当时该组件并不可得。典型：`002671` 的 2019 年中报/三季报 2020-01-03 才入库，而其 2018 年报 2020-04-29（2019 年报披露时）才入库。
- 处理：`PUB_prev_annual ≤ ACT_PUBTIME` 且 `PUB_prev_same ≤ ACT_PUBTIME` 才认可组件，否则 TTM 置 NaN（2,769 条）。这条把「用今天的眼光算过去的 TTM」彻底堵死。

### 处理结果

| 项目 | 数值 |
|---|---|
| 报告总数 | 148,820（年报 41,256 / 季度 107,564） |
| 年报 TTM NaN | 0（年报直接取本期，无需组件） |
| 季度 TTM NaN：组件缺失 | 17,783 |
| 季度 TTM NaN：组件发布晚于本期 | 2,769 |
| **TTM NaN 合计** | **20,552（13.81%）** |
| TTM ≤ 0（亏损，EP 保留为负） | 24,889 |

### 数据案例

- `000001` 平安银行 2020-03-31（Q1）：`TTM = 8.548e9 + 2.8195e10 − 7.446e9 = 2.9297e10`（发布时间 2020-04-20 18:42:38），与手算一致。

---

## 第 4 步：点时间对齐（PIT）

- 脚本：`代码/step4_点时间对齐_计算PE.py`（4~6 步合并）
- 方法：`signal_cutoff = date + 1天 − 1ns`（即 T 日 23:59:59.999999999），`pd.merge_asof(left=行情, right=报告, left_on='signal_cutoff', right_on='ACT_PUBTIME', by='code6', direction='backward')`。

### 口径说明：T 日收盘后切点

- **T 日行只用 T 日末及以前可知的信息，用于 T+1 日交易**。
- T 日盘后（如 19:02）披露的报告在 T+1 开盘前完全可知，故允许进入 T 日行——比旧口径（T 日零点、次日生效）**快 1 天**且仍满足「不允许使用开盘前不可获得的数据」。
- 发布时刻分布实测：94.7% 盘后（>15:00）、4.9% 盘前（<09:30）、0.4% 盘中——三类在 T+1 开盘前均可知，全部合规。
- 非交易日发布（周末/节假日）自然落入下一个交易日的切点内。

### 遇到的问题 → 处理方法

**问题 1：merge_asof 报错 `left keys must be sorted`**
- 处理：on 键必须是第一排序键。左侧按 `signal_cutoff`、右侧按 `ACT_PUBTIME` 全局排序，`by='code6'` 分组在内部完成。

**问题 2：同一发布时间有多条报告（本期 + 比较期）**
- 现象：某股同天发布「本期报告」与它带出的「去年同期比较期」，`ACT_PUBTIME` 相同（常精确到秒）。
- 处理：右侧排序 `['ACT_PUBTIME', 'code6', 'END_DATE']`，同时间戳内 `END_DATE` 更晚（真正的最新报告）排在最后，backward 匹配取到它。

**问题 3：合并改变行数 / 未来数据**
- 处理：硬校验——合并后行数必须等于行情行数；`ACT_PUBTIME > signal_cutoff` 的违例存在即报错。本次运行违例 = **0**。

### 数据案例（口径变更的直接体现）

| 案例 | 披露时刻 | 首个有效 signal 日期 | 说明 |
|---|---|---|---|
| `601398` 工商银行 2019 年报 | 2020-03-27（周五）21:09:09 | **2020-03-27** | 旧口径为 2020-03-30；新口径当日盘后披露进当日行，用于 2020-03-30（周一）交易 |
| `000001` 平安银行 2019 年报 | 2020-02-13 19:02:36 | **2020-02-13** | 旧口径为 2020-02-14 |

---

## 第 5 步：EP 计算

- 公式：`EP = TTM_ni / me_total`（分子分母均为元）。
- **保留负值与零**：亏损公司 EP 为负（信息保留、可排序），不再像 PE 那样置 NaN。
- 置 NaN 的情形：TTM 缺失（无可得报告或组件不完整）、`me_total ≤ 0` 或缺失、停牌日；次新标记不参与置空。
- 去极值/标准化不在本步做（原始因子值输出），EP 的极端值留待 MAD 处理。

### 为什么从 PE 改为 EP

1. PE 对亏损无定义、对微利爆炸（旧版 max≈53.9 万），MAD 截断前均值完全失真；EP 全程线性。
2. EP 与 PE 在盈利区间单调等价（EP = 1/PE），截面排序信息不损失。
3. 亏损股保留为负值后，覆盖率从 68.8% 提升到 88.6%，样本选择偏差消除（旧 PE 版会把约 142 万行亏损观察剔除）。

### 结果

| 指标 | 值 |
|---|---|
| 总行数 | 7,155,034 |
| 有效 EP | 6,342,144（**88.64%**） |
| 负值（亏损） | 1,416,492 |
| 零值 | 0 |
| 中位数（全体含负值） | 0.0209 |
| 正样本中位数 | 0.0293（≈ PE 34.2，与旧版 PE 中位数一致，口径连续性验证通过） |
| 最小值 / 最大值 | −26.31（300280，2025-10）/ 7.50 |

### NaN 归因（715.5 万行全景）

| 归因 | 行数 | 处理 |
|---|---|---|
| 停牌 | 22,675 | signal = NaN |
| 次新（非停牌） | 473,069 | signal = NaN |
| 核心样本中 TTM 不可得（报告未覆盖/组件缺失或晚到） | 317,146 | signal = NaN |
| **有效** | **6,342,144** | 88.64% |

（亏损不再是缺口——旧 PE 版核心缺口 173.5 万行中 81.5% 是亏损置 NaN，EP 已消除。）

### 年度覆盖率与 2020 年爬坡（点时间地平线的直观体现）

| 年份 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 |
|---|---|---|---|---|---|---|
| 有效率 | 67.1% | 87.1% | 89.6% | 91.4% | 95.4% | 95.4% |

| 2020 年 | 1 月 | 2 月 | 3 月 | 4 月 | 5 月起 |
|---|---|---|---|---|---|
| 有效率 | 0.1% | 0.5% | 5.7% | 41.4% | 89%~95% |

- 爬坡原因：库内财务报告发布时间起点为 2020-01-01，各股 2019 年报（法定 4 月 30 日前披露完）落地后覆盖率才饱和——**5 月起立即跳到 95%**，与披露节奏完全吻合，属点时间原则下的预期现象。
- 极端案例：2020-01-02 全市场 3,810 只股票仅 `002593` 一只有效（EP 0.0295 ≈ PE 33.9）——库内发布时间 ≤ 当日末的报告仅 9 条，其中仅 1 条构成完整 TTM。

---

## 第 6 步：输出

- 因子表：`数据/ep.parquet` 和 `数据/pe.parquet`（正式交付目录中对应 `因子结果\ep.parquet`、`因子结果\pe.parquet`）
  - `index = (date, stock_code)`、`columns = ['signal']`（signal = EP），date 为交易日，6 位纯代码。
- 掩码表：`数据/_mask.parquet`（同 index）
  - `suspended` / `suspended_unknown`：停牌与「停牌状态未知」
  - `is_cixin`：次新股
  - `st_filter_applied = 0` + `st_filter_note`：留痕「ST/PT 过滤因数据缺失未执行」

### 数据案例（EP 与 PE 对照）

| 股票 | 首个有效 | 末尾有效（2025-12-31） |
|---|---|---|
| `601398` 工商银行 | 2020-03-27 EP 0.1681（PE 5.95） | EP 0.1298（PE 7.71） |
| `000001` 平安银行 | 2020-02-13 EP 0.0992（PE 10.08） | EP 0.1947（PE 5.14） |

---

## 遗留事项（留痕，不影响本次输出）

1. **更正公告被忽略**：同一报告期只取最早发布值，公司后续更正（数值修正）不会进入因子——点时间安全，但属信息损失的设计取舍。
2. **`|EP|` 极端值**：300280 于 2025 年出现 EP −26.3（亏损 26 倍市值），大概率是微小市值 + 巨额一次性损益，待 MAD 去极值环节处理并复核。
3. **ST/PT 未剔除**：本地数据无历史 ST 标记，`_mask.parquet` 已留痕；如后续获得 ST 历史表，可回补过滤。
4. **股票池筛选**：已按行情表排除 50 只 `900xxx.BJ` B 股；其它交易所和板块不额外筛选，6 位代码在保留样本内无跨交易所碰撞。

---

## 附：EP / PE 分布直方图（2026-08-27）

- 脚本：`EP因子_交付版\代码\plot_ep_pe分布.py`（只读脚本，不修改任何管道输出）。
- 输出：`EP因子_交付版\图形\ep_原始分布.png`（池化 + 最新截面双面板）、`图形\pe_分布.png`（PE = 1/EP，同双面板）。
- 环境：matplotlib 3.11.0 已随 Python 3.12 安装，无需新增依赖。

### 统计（按交付版 `中间结果\ep_raw.parquet` 实测，与《EP因子交付说明》一致）

| 指标 | 全样本池化 | 2025-12-31 截面 |
|---|---|---|
| 有效观测 | 6,303,328 | 5,096 |
| 负值（亏损）占比 | 22.57% | 29.00% |
| EP 中位数 | 0.0207 | 0.0128 |
| EP 25% / 75% 分位 | 0.0027 / 0.0426 | −0.0043 / 0.0326 |
| PE 中位数（=1/EP 重新计算） | 25.11 | 26.80 |
| 正 PE 中位数 | 34.33 | 44.01 |

- 与第 5 步表的差异（有效 6,342,144 vs 本表 6,303,328 等）：第 5 步统计的是工作目录版本，交付版文件略有不同，以交付版实测为准（数字与交付说明吻合）。
- 交叉验证：正 PE 中位 34.33 ↔ 第 5 步「正样本中位数 0.0293 ≈ PE 34.2」一致；PE 极值 539,273 ↔ 第 5 步「旧版 PE max≈53.9 万」一致。

### EP → PE 非线性畸变（图上直观看得到）

1. **中位数不交换**：EP 中位 0.0207 对应 PE 48.3，但 PE 的中位数只有 25.1——因 1/x 在 0 处断裂，全部负 EP（22.6%）翻到 PE 负半轴成为「低 PE」，把中位数拉下来。
2. **近零爆炸**：|PE|>300 的观测占 8.04%（图窗口 [-100, 300] 截断显示），全部来自 EP 接近 0 的微利/微亏公司。
3. 2025-12-31 截面亏损占比 29.0%，高于 6 年池化的 22.6%。
